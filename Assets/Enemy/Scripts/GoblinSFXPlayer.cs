using UnityEngine;

namespace GJAM3
{
    public class GoblinSFXPlayer : SFXPlayer
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}